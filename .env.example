# Endpoint Google Apps Script nhận lead từ cả 2 form.
# Site chạy static export nên form gọi thẳng từ trình duyệt => biến phải có
# tiền tố NEXT_PUBLIC_ và giá trị sẽ lộ trong mã nguồn trang (chấp nhận được,
# vì URL này vốn đã công khai trong JS của bản WordPress cũ).
# Bỏ trống thì dùng URL mặc định trong src/lib/lead.ts.
NEXT_PUBLIC_SILICA_GAS_URL=
