# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# nhưng vẫn commit file mẫu
!.env.example

# bản zip landing cũ (bản cũ hơn docs/legacy-index.html, ảnh đã có ở public/img)
docs/silica-landing.zip

# zip logo gốc (ảnh đã giải nén vào public/img)
docs/drive-download-*.zip

# zip ảnh sản phẩm gốc (đã giải nén vào public/img)
docs/zips/
